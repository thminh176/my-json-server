{
  "name": "my-json-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "heroku-postbuild": "npm install json-server"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
