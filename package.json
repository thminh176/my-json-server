{
  "name": "my-json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "vercel-build": "json-server --watch db.json --port $PORT"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
