{
  "name": "my-json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server db.json --port $PORT", 
    "vercel-build": "npm run start" 
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
