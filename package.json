{
  "name": "my-json-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
